// loop through the files and check number of beams, fissions, and detectors
// finds the exceptions

{
  TChain* t_events = new t_events("midas_data", "Events in each file");

  for(int i = 0; i < 100)
}
